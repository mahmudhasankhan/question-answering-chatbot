version: '3'
services:
  question-answering-chatbot:
    image: question-answering-chatbot:latest
    build: ./app
    command: ["python", "main.py"]
    volumes:
      - ./:/app/
    ports:
      - "8000:8000"
